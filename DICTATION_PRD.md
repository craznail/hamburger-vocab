# 纸笔听写功能产品需求文档

> 版本：v0.2 · 日期：2026-06-09 · 状态：草案

---

## 1. 产品概述

### 1.1 一句话描述

听写功能提供“系统播报单词发音，用户用纸笔完成听写，最后手动查看答案”的轻量练习模式。

### 1.2 核心价值

- **低成本练习**：不要求用户在屏幕上输入，保留真实纸笔默写体验
- **专注听音与拼写**：播报时隐藏单词卡片，避免用户提前看到答案
- **复用现有词库**：直接使用今日待复习单词，不新增导入流程
- **不干扰复习计划**：首版不自动评分，不改变 SM-2 复习安排

---

## 2. 用户流程

### 2.1 今日听写

1. 用户打开首页，看到今日待复习数量
2. 点击“开始听写”
3. 系统进入听写页，默认隐藏单词和释义
4. 用户设置每个单词播报次数和播报间隔
5. 用户点击“开始播报”
6. 系统按设置播放当前单词发音
7. 用户在纸上写下听到的单词
8. 用户点击“看答案”，系统显示单词卡片
9. 用户手动切换到下一词，继续听写

### 2.2 指定词库听写

1. 用户进入词库详情页
2. 当该词库存在今日待复习单词时，点击“听写”
3. 系统进入 `/dictation?deckId={deckId}`
4. 听写队列仅包含该词库今日到期单词

---

## 3. 功能需求

### 3.1 入口

| ID | 功能 | 说明 | 优先级 |
|----|------|------|--------|
| D-01 | 首页听写入口 | 今日待复习数量大于 0 时展示“开始听写” | P0 |
| D-02 | 词库听写入口 | 词库详情页待复习数量大于 0 时展示“听写” | P0 |
| D-03 | 听写路由 | 新增 `/dictation`，支持可选 `deckId` 查询参数 | P0 |

### 3.2 播报控制

| ID | 功能 | 说明 | 优先级 |
|----|------|------|--------|
| D-04 | 开始播报 | 播放当前单词发音 | P0 |
| D-05 | 暂停播报 | 停止后续自动播报 | P0 |
| D-06 | 播报次数设置 | 每个单词播报 `1-5` 次，默认 `2` 次 | P0 |
| D-07 | 间隔时间设置 | 同一单词多次播报间隔 `1-10` 秒，默认 `3` 秒 | P0 |
| D-08 | TTS 兜底 | 优先 Web Speech API，失败后使用 Google Translate TTS | P0 |
| D-09 | 发音失败提示 | TTS 不可用时提示用户检查网络或系统 TTS 设置 | P0 |

### 3.3 答案展示

| ID | 功能 | 说明 | 优先级 |
|----|------|------|--------|
| D-10 | 默认隐藏答案 | 进入每个单词时不显示 `word`、`inflections`、`definition` | P0 |
| D-11 | 看答案 | 用户点击“看答案”后显示只读单词卡片 | P0 |
| D-12 | 切换后重置 | 切换上一词或下一词后重新隐藏答案 | P0 |
| D-13 | 不显示评分 | 听写页不展示“忘了 / 模糊 / 掌握”按钮 | P0 |

### 3.4 队列与完成

| ID | 功能 | 说明 | 优先级 |
|----|------|------|--------|
| D-14 | 今日队列 | 复用现有今日待复习队列 | P0 |
| D-15 | 指定词库队列 | `deckId` 存在时，只取该词库今日到期单词 | P0 |
| D-16 | 上一词 / 下一词 | 用户手动切换听写项 | P0 |
| D-17 | 完成页 | 最后一词后展示“本轮播报完成” | P0 |
| D-18 | 再来一轮 | 完成后可从第一词重新开始 | P0 |
| D-19 | 空状态 | 今日无待复习单词时展示空状态 | P0 |

---

## 4. 页面设计

### 4.1 首页入口

```
今日待复习
12 张卡片需要今天复习

[开始学习] [开始听写]
```

### 4.2 词库详情入口

```
词库名称

[学习] [听写]
```

仅当该词库待复习数量大于 0 时展示入口。

### 4.3 听写页

答案隐藏状态：

```
3 / 20

播报设置
播报次数：2
间隔秒数：3

[发音状态图标]
请听发音并在纸上写下单词

[开始播报] [看答案]

[上一词] [下一词]
```

答案展示状态：

```
apple
apples
苹果
```

完成状态：

```
本轮播报完成
共播报 20 个单词

[再来一轮] [返回首页]
```

---

## 5. 技术要求

### 5.1 前端

- 新增 `DictationPage.vue`
- 新增路由：
  - `name: 'Dictation'`
  - `path: '/dictation'`
  - `query.deckId` 可选
- 复用 `store.getTodayLearningCards(deckId)` 获取队列
- 抽出公共发音工具，供卡片学习、词库详情、听写页共用
- 播报设置只保存在页面本地状态，刷新后恢复默认值

### 5.2 数据层

- 不新增 Tauri command
- 不新增数据库表或字段
- 不调用 `rateCard`
- 不写入复习日志
- 不改变今日待复习数量

---

## 6. 非功能需求

| 类别 | 要求 |
|------|------|
| 响应式 | 适配手机和桌面 |
| 专注性 | 播报时页面不能出现单词答案 |
| 可控性 | 用户可随时暂停、切换上一词或下一词 |
| 容错 | 空队列和 TTS 失败都有明确提示 |
| 一致性 | 视觉风格延续当前卡片学习页面 |

---

## 7. 验收标准

1. 首页今日待复习大于 0 时，“开始听写”可进入听写页
2. 词库详情页待复习大于 0 时，“听写”可进入指定词库听写页
3. 听写页加载后不显示单词、词形或释义
4. 点击“开始播报”后，当前单词按设置次数播放
5. 多次播放之间遵守设置的间隔秒数
6. 修改播报次数和间隔后，对后续播报生效
7. 点击“看答案”后显示单词、词形和释义
8. 切换上一词或下一词后，答案重新隐藏
9. 听写流程不调用评分接口，不改变复习计划
10. 今日无待复习时展示空状态，不报错

---

## 8. 后续迭代

- 支持听写全部单词
- 支持保存播报设置
- 支持自动连续播报下一词
- 支持用户自行标记听写结果
- 支持听写专项统计
