# hamburger-vocab 技术路线与风险控制

> 版本：v0.1 · 日期：2026-06-16 · 状态：基线约束

本文档用于约束 `hamburger-vocab` 后续功能演进，尤其是错题、同步、AI、图片和本地数据库相关修改。目标不是把实现固定死，而是避免后续改动破坏“本地优先 + 云端同步 + 服务端 AI”的核心路线。

---

## 1. 总体路线

`hamburger-vocab` 的长期架构是：

```text
Tauri App
  Vue UI
  Rust commands/service
  本地 SQLite
  本地文件缓存

        ↕ HTTPS API

远程服务端
  用户身份
  AI 代理
  云端主副本
  图片对象存储
  同步协议
```

核心原则：

- App 是本地优先的学习客户端。
- 本地 SQLite 是当前设备的工作副本。
- 远程服务端是跨设备数据主副本。
- AI Key、Prompt、模型选择、限流和成本控制必须放在服务端。
- 图片云端保存主副本，本地只保存缓存或待上传草稿。
- 复习必须支持本地即时完成，结果后台同步。

不推荐路线：

- 不要把 App 做成纯 WebView 网页壳。
- 不要让 Vue 直接调用 OpenAI/Gemini 等 AI API。
- 不要把云端 URL 当作唯一图片来源，本地至少要支持缓存和离线草稿。
- 不要只同步最终状态而丢弃复习日志。

---

## 2. 本地与服务端边界

### App 本地负责

- 文件选择、拍照、图片预览、基础压缩。
- 图片本地缓存，路径位于 `app_data_dir/images/...`。
- 本地 SQLite 表结构、查询、离线草稿和同步状态。
- 单词卡片和错题复习的 SM-2 计算。
- 复习记录本地写入和待同步队列。
- 同步触发、失败重试、同步状态展示。
- UI 体验：列表、详情、编辑、复习、离线提示。

### 远程服务端负责

- 登录、Token 签发、刷新和鉴权。
- AI 分析代理，不向 App 暴露 AI Key。
- 图片主副本存储，第一版可为服务端本地目录，后续可切对象存储。
- 云端错题、复习日志、用户数据的主副本。
- 多设备增量同步、软删除传播和冲突合并。
- 限流、审计日志、AI 成本控制。

### 禁止跨界

- App 不保存第三方 AI Key。
- App 不直接访问第三方 AI 服务。
- 服务端不参与每一次本地翻卡动画、复习页面状态或低价值 UI 状态。
- App 不应假设服务端永远在线。

---

## 3. 同步协议基线

所有需要跨设备同步的数据必须具备：

- `local_id`：本地 ID，离线创建时即可生成。
- `remote_id`：服务端 ID，上传成功后写入。
- `updated_at`：最后修改时间。
- `deleted_at`：软删除时间，不能直接硬删后丢失同步语义。
- `version`：服务端版本号，由服务端递增。
- `sync_status`：本地同步状态。

推荐状态：

```text
local_draft       本地草稿，尚未上传
pending_analyze   已保存图片，等待 AI 分析
analyzing         正在调用服务端 AI
analyze_failed    AI 分析失败，可重试
pending_sync      本地有变更，等待同步
synced            已同步
deleted           已软删除
```

同步触发时机：

- App 启动后。
- 用户登录后。
- 网络恢复后。
- 用户手动点击同步。
- 复习完成后延迟后台同步。

同步顺序固定为：

```text
1. push 本地 pending 变更
2. pull 服务端 since lastSyncAt 后的增量
3. 合并写入本地 SQLite
4. 更新 lastSyncAt / cursor
```

冲突规则第一版：

- 服务端 `deleted_at` 优先级最高。
- 题干、答案、解析、图片等 AI 生成字段以服务端为准。
- 用户笔记和复习状态以较新的 `updated_at` 为准。
- 复习记录必须作为日志追加，不应只同步最终 `mastery_level`。
- 如果本地记录没有 `remote_id`，它是本地草稿，不参与远程冲突。

后续如果引入更复杂同步，必须保持向后兼容旧版本本地数据库。

---

## 4. 图片与文件策略

图片不能长期只存在本地，也不要直接把大图 base64 存入 SQLite。

本地保存：

```text
app_data_dir/
  vocab.db
  images/
    error-items/
      {sha256}.jpg
```

本地数据库保存：

- `local_path`
- `remote_key`
- `remote_url`
- `sha256`
- `mime_type`
- `upload_status`

服务端保存：

- 数据库只保存图片元数据和对象 key。
- 图片文件存服务端本地目录或对象存储。
- 后续切换 MinIO/S3/R2/OSS 时，不应影响 App API。

风险控制：

- 上传前计算 `sha256`，用于去重和重试。
- 本地缓存丢失时，应允许通过 `remote_key` 重新下载。
- 远程 URL 可能过期，App 不应只依赖永久 URL。
- 删除错题时，第一版先软删除数据库记录，图片垃圾回收可后置。

---

## 5. 数据库迁移纪律

Tauri App 发布后，用户本地会存在不同历史版本的 SQLite 数据库。任何数据库修改都必须可增量升级。

要求：

- 只做向前兼容迁移。
- 新字段必须有默认值或允许 `NULL`。
- 不直接删除旧字段。
- 不直接重命名字段；如必须重命名，先新增字段并迁移数据。
- 不重建表导致用户数据丢失。
- 查询必须容忍旧数据中字段为空。

建议尽快引入：

```sql
CREATE TABLE IF NOT EXISTS schema_migrations (
  version TEXT PRIMARY KEY,
  applied_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

后续每个迁移应有稳定版本号，并且可重复执行。

---

## 6. AI 调用策略

AI 能力统一走服务端：

```text
App
  上传图片 / 草稿 ID
  接收结构化结果

服务端
  读取 AI 配置
  构造 Prompt
  调用 Gemini / OpenAI / Azure
  校验结果
  保存云端主副本
```

服务端必须负责：

- API Key 管理。
- Prompt 版本管理。
- 模型切换。
- 超时和重试。
- 统一错误码。
- 用户限流和成本控制。

App 只需要关心：

- 正在分析。
- 分析成功。
- 分析失败，可重试。
- 离线保存为待分析草稿。

---

## 7. 未来功能扩展顺序

### 阶段 1：单设备闭环

- 本地错题草稿。
- 服务端 AI 分析。
- 本地保存分析结果。
- 本地复习和复习日志。

### 阶段 2：基础同步

- 新增错题同步。
- 复习日志同步。
- 远程错题增量拉取。
- 软删除传播。
- 服务端胜出的简单冲突策略。

### 阶段 3：可靠同步

- append-only 复习日志。
- 字段级冲突合并。
- 图片上传/下载重试队列。
- 同步状态页。
- 失败恢复和手动重试。

### 阶段 4：体验增强

- 多设备一致性优化。
- 后台自动同步。
- 对象存储签名 URL。
- 本地缓存清理。
- 数据备份恢复。
- 统一“今日学习任务”：单词 + 错题。

---

## 8. 修改前检查清单

涉及同步、数据库、图片、AI、登录的改动，提交前必须回答：

- 这个改动是否影响离线使用？
- 本地数据和服务端数据谁是主副本？
- 是否需要新增 `sync_status` 或迁移旧状态？
- 旧版本 SQLite 数据库能否无损升级？
- 图片本地缓存丢失时能否恢复？
- 多设备同时修改时结果是否可解释？
- AI Key 是否仍只存在服务端？
- 失败后用户是否可以重试，且不丢数据？

---

## 9. 验收门槛

相关功能上线前至少验证：

- 无网时可创建本地草稿。
- AI 分析失败后草稿仍保留。
- 重新打开 App 后草稿仍可重试。
- 复习评分立即更新本地下一次复习时间。
- 复习记录能在联网后同步。
- 远程新增错题能被本地 pull 到。
- 服务端软删除能传播到本地。
- 图片不以 base64 大字段长期存 SQLite。
- App 不包含第三方 AI API Key。

---

## 10. 当前基线说明

当前 MVP 已确立以下边界：

- `hamburger-vocab` 继续使用 Tauri Rust 管本地 SQLite 和远程通信。
- Vue 不直接请求远程服务。
- `wrong-notebook` 作为第一版远程服务端。
- 错题复习本地执行，复习日志后台同步。
- AI 分析只能在线完成，离线只保存待分析草稿。
- 标签第一版保存为字符串数组，暂不强制层级标签树。

后续如果偏离这些边界，应先更新本文档，并说明迁移策略和回滚方案。
