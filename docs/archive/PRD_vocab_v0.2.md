# 单词卡片学习系统 — 产品需求文档

> 版本：v0.2 · 日期：2026-06-06 · 状态：草案

---

## 1. 产品概述

### 1.1 一句话描述

一款基于 Web 的单词学习应用。用户上传 txt 文件导入单词，以卡片形式逐个学习，通过间隔重复算法智能安排复习节奏，高效掌握词汇。

### 1.2 核心价值

- **零门槛录入**：txt 文件上传即用，无需手动添加
- **卡片化学习**：一次一个单词，专注不 overload
- **科学复习**：间隔重复算法确保在遗忘临界点提醒，避免无效重复

### 1.3 目标用户

- **英语学习者**：备考（四六级/考研/雅思/托福）、日常积累词汇
- **老师/教育者**：为学生批量导入单词表
- **自学者**：学习任何语言（西语、日语等）的词汇

---

## 2. 用户流程（核心场景）

```
上传文件 → 解析预览 → 开始学习 → 看卡片 → 回忆/评分 → 算法安排下次复习
```

### 场景一：首次使用

1. 用户打开应用，看到空状态页面
2. 点击"上传文件"按钮，选择本地的 txt 文件
3. 系统解析文件，展示预览（单词数量、格式识别结果）
4. 用户确认导入
5. 自动进入学习模式，看到第一张卡片

### 场景二：日常学习

1. 用户打开应用，看到今日待复习数量
2. 点击"开始学习"进入卡片模式
3. 看到单词正面 → 尝试回忆 → 点击翻转
4. 看到释义 → 自评掌握程度（3 档）
5. 系统记录结果并计算下次复习时间
6. 进入下一张卡片

### 场景三：复习回顾

1. 用户在首页看到所有单词本概览
2. 选择某个单词本，查看统计（总词数、已掌握、待复习）
3. 可筛选查看"已掌握"和"待复习"的单词列表

---

## 3. 功能需求

### 3.1 单词上传与解析

| ID | 功能 | 说明 |
|----|------|------|
| F-01 | 文件上传 | 支持 .txt 格式，拖拽或点击上传 |
| F-02 | 格式智能解析 | 自动识别以下三种格式（详见 3.1.1） |
| F-03 | 预览确认 | 上传后展示解析结果（单词数、样例），用户确认后再入库 |
| F-04 | 重复检测 | 若单词已在词库中，提示并可选跳过或覆盖 |
| F-05 | 文件名作为词库名 | 上传后默认以文件名创建词库（单词本） |

#### 3.1.1 支持的 txt 格式

**格式 A — 纯单词列表（每行一个单词）**

```
apple
banana
go
```

> 系统自动通过词典 API 查询释义、音标、词形变化。

**格式 B — 单词 + 释义**

```
apple 苹果
go 去
run 跑
```

> 分隔符为空格或 Tab；系统解析释义，无需额外查询。

**格式 C — 单词 + 词形变化 + 释义**

```
go went gone 去
run ran run 跑
eat ate eaten 吃
```

> 系统解析词形变化（过去式、过去分词等），自动关联同一单词的不同形式在同一张卡片展示。

**格式自动识别逻辑**

1. 统计一行中由空格/Tab 分隔的字段数
2. 若均为 1 字段 → 格式 A
3. 若均为 2 字段 → 格式 B（第 1 字段为单词，第 2 字段为释义）
4. 若出现 3+ 字段 → 格式 C（第 1 字段为原形，中间字段为词形变化，最后字段为释义）
5. 行内格式不一致时，以简单多数为准，不一致的行单独标红提示

### 3.2 单词卡片学习

| ID | 功能 | 说明 |
|----|------|------|
| F-06 | 卡片翻转 | 正面显示单词（及词形变化），点击翻转显示释义 |
| F-07 | 发音 | 点击单词可播放发音（TTS） |
| F-08 | 评分按钮 | 翻转后出现三档评分按钮 |
| F-09 | 学习进度 | 当前第几张 / 今日共几张 |
| F-10 | 今日待复习 | 首页展示今日到期卡片数 |
| F-11 | 学习即复习 | 上传新词后，新词与到期复习词合并为当日学习队列 |

#### 3.2.1 卡片设计

**卡片正面**

```
┌─────────────────────┐
│                     │
│        go           │
│    went · gone      │
│                     │
│      [🔊 发音]      │
│                     │
└─────────────────────┘
```

**卡片背面**

```
┌─────────────────────┐
│        go           │
│    went · gone      │
│                     │
│       去 / 走        │
│                     │
│  ┌────┐ ┌────┐ ┌──┐│
│  │忘了│ │模糊│ │✓ ││
│  └────┘ └────┘ └──┘│
└─────────────────────┘
```

#### 3.2.2 三档评分

| 按钮 | 含义 | 对算法的影响 |
|------|------|-------------|
| 忘了 | 完全想不起来或答错 | 重置间隔，本次学习质量 = 0 |
| 模糊 | 回忆起来但不确定 / 花了较长时间 | 保持或轻微增加间隔，质量 = 3 |
| ✓ 掌握 | 准确回忆，很确定 | 正常增加间隔，质量 = 5 |

### 3.3 间隔重复算法

采用 SM-2 算法的简化适配版。

#### 3.3.1 核心参数

| 参数 | 说明 | 初始值 |
|------|------|--------|
| EF | 难易系数 (Ease Factor)，范围 1.3 – 2.5 | 2.5 |
| interval | 下次复习间隔（天） | 1 |
| repetitions | 连续"掌握"次数 | 0 |
| next_review | 下次复习日期 | 当天 |
| quality | 每次评分质量（0、3、5） | — |

#### 3.3.2 算法逻辑

```
根据 quality 计算：

- quality = 0（忘了）
  → repetitions = 0
  → interval = 1 天
  → EF -= 0.2（下限 1.3）

- quality = 3（模糊）
  → repetitions = 0（归零，不累积连续掌握）
  → interval = 1 天
  → EF -= 0.15（下限 1.3）

- quality = 5（掌握）
  → repetitions += 1
  → 若 repetitions == 1：interval = 1 天
  → 若 repetitions == 2：interval = 6 天
  → 若 repetitions >= 3：interval = CEIL(interval * EF)
  → EF += 0.1（上限 2.5）

next_review = DATE('now', '+' || interval || ' days')
```

#### 3.3.3 每日学习队列排序

1. 所有 `next_review <= today` 的卡片
2. 按 `ef` 升序（难的优先）
3. 同 `ef` 按 `next_review` 升序（到期最早的优先）
4. 同一天到期的，新词排在复习词后面
5. 上限：每天最多学习 50 张卡片（可配置）

### 3.4 数据统计

| ID | 功能 | 说明 |
|----|------|------|
| F-12 | 词库总览 | 每个词库的单词总数、已掌握数、待复习数 |
| F-13 | 学习详情 | 每日学习记录条数、掌握率趋势 |
| F-14 | 词库管理 | 可删除词库、清空已完成 |
| F-15 | 导出 | 将已掌握的单词导出为 txt |

#### 3.4.1 统计看板设计

首页内容：

- **今日待复习**：大数字展示（到期卡片数）
- **词库列表**：卡片式，显示 词库名 / 总数 / 掌握数 / 进度条
- **快捷入口**：开始今日学习 / 上传新词库

### 3.5 数据管理

| ID | 功能 | 说明 |
|----|------|------|
| F-16 | 持久化 | 所有数据存储在浏览器 localStorage 中的 sql.js 数据库快照 |
| F-17 | 数据备份 | 支持将数据库文件（.sqlite）下载到本地 |
| F-18 | 数据恢复 | 支持上传之前下载的 .sqlite 文件恢复数据 |
| F-19 | 搜索 | 在词库内搜索单词 |

---

## 4. UI/UX 设计准则

### 4.1 设计风格

- **安静、专注**：大留白、低干扰、中性色为主
- **卡片为核心**：卡片是页面上唯一需要聚焦的元素
- **移动优先响应式**：同时适配桌面和手机端

### 4.2 页面结构

| 页面 | 路由 | 内容 |
|------|------|------|
| 首页 | `/` | 今日待复习数、词库列表、上传按钮、统计概览 |
| 学习页 | `/study?deckId=xxx` | 全屏卡片、翻转按钮、评分按钮、进度条 |
| 词库详情页 | `/deck/:id` | 单词列表、筛选（已掌握/待复习）、搜索框 |

### 4.3 交互细节

- 卡片翻转支持点击和键盘快捷键（空格/Enter）
- 评分按钮支持快捷键（1=忘了，2=模糊，3=掌握）
- 学习完成后展示当日小结（学了多少、掌握率）
- 空状态时展示引导文案和示意插画

### 4.4 配色参考（提议）

- 背景：`#fafafa`
- 卡片背景：白色，圆角 12px，轻微阴影
- 主色：`#2563eb`（蓝色，用于按钮和链接）
- 忘了：`#ef4444`（红色）
- 模糊：`#f59e0b`（黄色/琥珀色）
- 掌握：`#22c55e`（绿色）

---

## 5. 数据库设计（SQLite）

### 5.1 词库表（`decks`）

```sql
CREATE TABLE decks (
    id          TEXT PRIMARY KEY,     -- UUID
    name        TEXT NOT NULL,        -- 词库名称（默认文件名）
    created_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
```

### 5.2 单词卡片表（`cards`）

```sql
CREATE TABLE cards (
    id            TEXT PRIMARY KEY,           -- UUID
    deck_id       TEXT NOT NULL,              -- 所属词库 ID
    word          TEXT NOT NULL,              -- 单词（原形）
    inflections   TEXT DEFAULT '',            -- 词形变化，JSON 数组字符串，如 '["went","gone"]'
    definition    TEXT DEFAULT '',            -- 释义
    ef            REAL NOT NULL DEFAULT 2.5,  -- 难易系数 1.3–2.5
    interval      INTEGER NOT NULL DEFAULT 1, -- 当前间隔（天）
    repetitions   INTEGER NOT NULL DEFAULT 0, -- 连续掌握次数
    next_review   TEXT NOT NULL DEFAULT (date('now')), -- 下次复习日期
    created_at    TEXT NOT NULL DEFAULT (datetime('now')),
    last_review_at TEXT,                      -- 上次复习时间

    FOREIGN KEY (deck_id) REFERENCES decks(id) ON DELETE CASCADE
);

CREATE INDEX idx_cards_next_review ON cards(next_review);
CREATE INDEX idx_cards_deck_id ON cards(deck_id);
```

### 5.3 学习记录表（`review_logs`）

```sql
CREATE TABLE review_logs (
    id            TEXT PRIMARY KEY,     -- UUID
    card_id       TEXT NOT NULL,        -- 对应卡片 ID
    reviewed_at   TEXT NOT NULL DEFAULT (datetime('now')),
    quality       INTEGER NOT NULL,     -- 评分 (0/3/5)
    ef_before     REAL NOT NULL,
    ef_after      REAL NOT NULL,

    FOREIGN KEY (card_id) REFERENCES cards(id) ON DELETE CASCADE
);

CREATE INDEX idx_review_logs_card_id ON review_logs(card_id);
```

---

## 6. 技术方案

| 维度 | 选择 |
|------|------|
| 框架 | Vue 3 + Vite |
| 路由 | Vue Router 4 |
| 状态管理 | Pinia |
| 本地数据库 | sql.js（SQLite WebAssembly，持久化到 localStorage） |
| 样式 | Tailwind CSS |
| 发音 | Web Speech API（SpeechSynthesisUtterance） |
| 词典 API | 可选：Free Dictionary API 或本地词典数据 |
| 部署 | Vercel / Netlify / GitHub Pages |

### 6.1 数据持久化策略

sql.js 将 SQLite 数据库加载到内存中操作，通过以下方式持久化：

```
启动 → 从 localStorage 读取 DB 快照（Uint8Array）
      → 加载到 sql.js 内存
      → 正常操作（增删改查）
      → 每次写操作后，将内存 DB 导出为 Uint8Array
      → 写入 localStorage
```

> MVP 暂不涉及自动文件同步。用户可通过"下载 .sqlite 文件"手动备份。

---

## 7. 版本规划

| 版本 | 功能 | 目标 |
|------|------|------|
| MVP v0.1 | 文件上传 + 卡片学习 + 间隔重复 + 今日统计 | 核心闭环可用 |
| v0.2 | 键盘快捷键 + 发音 + 搜索 + 数据导入导出 | 提升效率 |
| v0.3 | 学习记录趋势图 + 多词库管理 | 管理与洞察 |
| v0.4 | 自定义评分档位 + 浏览器通知提醒 | 个性化 |
| v1.0 | 暗色模式 + PWA（离线可用 + 安装到桌面） | 完善体验 |

---

## 8. 开放问题 / 待讨论

- 纯单词列表（格式 A）是否需要联网查询释义？默认开启，可选关闭
- 是否支持多语言（不只是英语）？可以，TTS 语言自动匹配
- 手机端的触摸交互：左右滑动切换卡片 vs 点击翻转 + 点击评分
- 是否希望加入词根词缀、例句等额外信息？（可在 v0.3+ 考虑）
